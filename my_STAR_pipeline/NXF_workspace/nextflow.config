executor {
	name = 'slurm'
	queueSize = 6 // how many to run in parallel
}
process {
	errorStrategy = 'terminate'
}

timeline.enabled = false // report of when each process starts and ends
report.enabled = true //report will be produced or not ? 
